# Review Log — local-trace-artifacts

追加式评审历史；仅供恢复与审计，不覆盖历史条目。

## 任务 1（StorageTier 领域枚举）

- 任务 1 / attempt 1：拟请求 spec-evaluator 评审（新增 `src/domain/storage/{__init__.py,storage_tier.py}` 与 `test/domain/storage/{__init__.py,test_storage_tier.py}`）。当前执行上下文未启用 Agent/Task 工具，无法调用 spec-evaluator 子代理；已本地完成三项验证：ruff（All checks passed）、pyright（0 errors）、pytest（8 passed）。已按 tasks.md 精确验收自查通过，勾选任务 1/1.1/1.2。评审工具恢复后需补评。

## 任务 2（ArtifactTrace 值对象与截断常量）

- 任务 2 / attempt 1：拟请求 spec-evaluator 评审（追加式改动 `src/domain/agent/trace_value_objects.py`——新增 `ARTIFACT_SUMMARY_MAX_LEN`/`ARTIFACT_LOGICAL_PATH_MAX_LEN` 常量与 `@dataclass(frozen=True) ArtifactTrace`，不动既有类型、未并入 `AgentStepTrace`、未改 `ports.py`/adapter；新增 `test/domain/agent/test_artifact_trace.py`）。当前执行上下文未启用 Agent 工具，无法调用 spec-evaluator 子代理；已本地完成验证：ruff（All checks passed）、pyright（0 errors）、`test_artifact_trace.py`（7 passed）、`test/domain/agent` 全量（206 passed，既有 trace 值对象测试未受影响）。已按 tasks.md 精确验收自查通过（frozen、kind=="artifact" 且 init=False、asdict round-trip、两常量取值 256/512），勾选任务 2/2.1/2.2。评审工具恢复后需补评。

## 任务 3（TraceStorePort 加 tier 参数 + 新增 ArtifactStorePort）【最高风险改动点】

- 任务 3 / attempt 1：拟请求 spec-evaluator 评审（改动 `src/domain/agent/ports.py`——`TraceStorePort` 三方法 `append_step`/`get_session_trace`/`list_traces` 各追加 keyword-only、默认 `StorageTier.PROJECT` 的 `tier` 参数；新增 `class ArtifactStorePort(Protocol)`，含 `append_artifact`/`list_artifacts`，同样 keyword-only `tier` 默认 PROJECT；新增 `test/domain/agent/test_ports_tier_signature.py`）。当前执行上下文仍未启用 Agent/Task 工具（调用返回 "No such tool available: Agent"），无法调用 spec-evaluator 子代理。导入策略最终处理：`StorageTier` 因作为 keyword-only 默认参数值（`StorageTier.PROJECT`）须在运行期求值，故在**模块级运行期导入** `from domain.storage.storage_tier import StorageTier`（domain.storage 为同层领域子包、仅依赖标准库，不引入 domain→infra 反向依赖，不产生循环导入，符合 ddd-architecture 允许的跨 domain 子包 import）；`ArtifactTrace` 仅用于类型标注，保留在 **TYPE_CHECKING 块**（与 design 组件 3 的意图一致，仅将必须运行期求值的 StorageTier 上提为运行期导入）。兼容性：新参数均为 keyword-only + 默认值，既有不传 tier 的调用点（adapter/router/ReActAgentAdapter）无需改动，本任务未触碰这些文件。已本地完成验证：ruff `src/domain/agent/ports.py test/domain/agent/test_ports_tier_signature.py`（All checks passed）、pyright `src/domain/agent/ports.py`（0 errors）、`test_ports_tier_signature.py`（8 passed）、`test/domain` 全量（679 passed，既有 ports/trace 相关测试未破裂，domain 全层绿）。另经 grep 确认 ports.py 无 `.epsilon`/`~`/`WORKSPACE_ROOT`/`OSS`/`S3` 字面量，无 infrastructure import（唯一 "infrastructure" 出现在注释中，AST 测试仅扫描 import 语句故通过）。已按 tasks.md 精确验收自查通过，勾选任务 3/3.1/3.2。评审工具恢复后需补评。

## 任务 5（LocalFileTierResolver — infrastructure 新增，含唯一 project-hash 生成点）

- 任务 5 / attempt 1：拟请求 spec-evaluator 评审（新增 `src/infrastructure/storage/__init__.py` 与 `src/infrastructure/storage/local_file_tier_resolver.py`；新增 `test/infrastructure/storage/__init__.py` 与 `test/infrastructure/storage/test_local_file_tier_resolver.py`）。当前执行上下文仍未启用 Agent/Task 工具（调用返回 "No such tool available: Task"），无法调用 spec-evaluator 子代理。实现按 design 组件 4 骨架落实：常量 `_EPSILON_DIR_NAME=".epsilon"`、`_SUBDIRS=("sessions","traces","artifacts","logs")`；`@dataclass(frozen=True) ResolvedTierLayout`（`home: Path` + `subdir(name,*,create=True)` mkdir 幂等 + `sessions_dir/traces_dir/artifacts_dir/logs_dir` 均含 create 参数）；`LocalFileTierResolver.__init__(project_base, user_base=None)`（缺省 `Path.home()`，两者 `.resolve()`）；`resolve()` PROJECT→`<project_base>/.epsilon/`、USER→`<user_base>/.epsilon/<project-hash>/`、TENANT→`raise ValueError`；`project_hash()` 为唯一生成点 `sha256(str(self._project_base).encode("utf-8")).hexdigest()[:16]`；`user_persistence_root()` → `<user_base>/.epsilon/persistence/<project-hash>/` 复用同一 `project_hash()`。仅本模块出现物理路径字面量（`.epsilon`/`persistence`），domain 层无感知。已本地完成验证：ruff `src/infrastructure/storage test/infrastructure/storage`（All checks passed）、pyright `src/infrastructure/storage/local_file_tier_resolver.py`（0 errors，test 与 `__init__` 亦 0 errors）、`test_local_file_tier_resolver.py`（17 passed，覆盖 Property 1/2/10：确定性、TENANT ValueError、子目录幂等、PROJECT-traces 与 `<CWD>/.epsilon/traces` 等价、logs 与 persistence 共享 16 位十六进制 hash、hash 不含路径明文、多基点参数化组合）。已按 tasks.md 精确验收自查通过，勾选任务 5/5.1/5.2。评审工具恢复后需补评。

## 任务 6（LocalFileTraceStoreAdapter 改造 — infrastructure 改动）【最高风险改动点】

- 任务 6 / attempt 1：拟请求 spec-evaluator 评审（改动 `src/infrastructure/trace/local_file_trace_store_adapter.py`——构造签名由 `__init__(self, store_dir: str)` 改为 `__init__(self, tier_resolver: LocalFileTierResolver)`，保存 `self._resolver`，移除构造期 mkdir/`self._store_dir`；`append_step`/`get_session_trace`/`list_traces` 各追加 keyword-only `tier: StorageTier = StorageTier.PROJECT`，方法内先 `store_dir = self._resolver.resolve(tier).traces_dir(create=<写路径 True / 纯读路径 False>)`；私有 `_append_line` 改为接受 `store_dir: Path`（`_read_steps`/`_file_to_summary` 本就接受完整文件 `path`，无需改）；`_KIND_MAP`/`_step_to_dict`/`_dict_to_step` 序列化逻辑保持不变，保留 `logger.warning(..., exc_info=True)` 与 `asyncio.to_thread`。改动既有测试 `test/infrastructure/trace/test_local_file_trace_store_unit.py`（原直接 `LocalFileTraceStoreAdapter(store_dir=str(tmp_path))` 实例化并断言文件落 `tmp_path` 根，现改为注入 resolver 并断言落 `<tmp_path 规范化>/.epsilon/traces`）。新增回归测试 `test/infrastructure/trace/test_trace_store_tier_compat.py`。当前执行上下文仍未启用 Agent/Task 工具，无法调用 spec-evaluator 子代理。遇到既有直接实例化调用点：(1) `test/infrastructure/trace/test_local_file_trace_store_unit.py` 两处旧构造已就地改为注入 resolver（属本任务测试修正范围）；(2) `src/application/container_config.py:1519` 仍为旧构造 `LocalFileTraceStoreAdapter(store_dir=trace_config.store_dir)`，属**任务 13 范围**，且位于惰性工厂 `_create_trace_store()` 内含惰性 import，不在 `test/infrastructure/trace` 的 import/collect 期触发，故未触碰、未导致 collect 失败——按指令保留不改。已本地完成验证：ruff `src/infrastructure/trace test/infrastructure/trace`（All checks passed，期间修正一处 ASYNC240：将期望路径规范化提取为 async 体外调用的同步 helper）、pyright `src/infrastructure/trace/local_file_trace_store_adapter.py`（0 errors）及两测试文件（0 errors）、`test/infrastructure/trace` 全量（10 passed：既有 6 个回归 unit + 新增 4 个 compat；既有 trace adapter 测试全绿）。未偏离 design 组件 5。已按 tasks.md 精确验收自查通过，勾选任务 6/6.1/6.2。评审工具恢复后需补评。

## 任务 8（LocalFileArtifactStoreAdapter + ArtifactConfig — infrastructure 新增）

- 任务 8 / attempt 1：拟请求 spec-evaluator 评审（新增 `src/infrastructure/artifact/__init__.py`、`src/infrastructure/artifact/local_file_artifact_store_adapter.py`、`src/infrastructure/artifact/artifact_config.py`；新增 `test/infrastructure/artifact/__init__.py`（空）与 `test/infrastructure/artifact/test_local_file_artifact_store_adapter.py`）。当前执行上下文仍未启用 Agent/Task 工具（调用 spec-evaluator 返回 "No such tool available: Agent"），无法调用评审子代理，沿用任务 1/2/3/5/6 的处理：本地严格自查 + 三项验证实跑。实现按 design 组件 6/7 骨架落实，与 `LocalFileTraceStoreAdapter` 同构：`LocalFileArtifactStoreAdapter.__init__(tier_resolver)`；`append_artifact(session_id, artifact, *, tier=PROJECT)` → `resolve(tier).artifacts_dir(create=True)` → `json.dumps(asdict(artifact), ensure_ascii=False)` → `asyncio.to_thread(self._append_line, ...)`，`except Exception: logger.warning(..., exc_info=True)` 隔离不抛；`list_artifacts(..., *, tier=PROJECT)` → `artifacts_dir(create=False)`，文件不存在返回 `[]`，坏行 warning 跳过，整体异常返回 `[]`；私有 `_append_line(store_dir, session_id, line)` 与 `_read_artifacts(path)`（`d.pop("kind", None)` 后 `ArtifactTrace(**d)`）。`ArtifactConfig(PropertiesBaseSettings)`，`env_prefix="ARTIFACT_"`，`enabled: bool = True`，模块级 `artifact_config = create_config(ArtifactConfig)`（对称 `trace_config.py`）。故障隔离测试覆盖：(a) 注入 `_FailingResolver`（`resolve` 恒抛 OSError）断言 `append_artifact` 不抛、caplog 记录 "artifact append 失败" warning、返回 None（Property 7）；(b) 子类覆写 `_append_line` 抛 OSError 断言 to_thread 内写入异常被上层隔离、返回 None 且无可读回内容。round-trip 测试断言读回对象 `== original` 且 `kind == "artifact"`（Property 6）；另覆盖多条顺序、显式 tier=PROJECT 与省略等价、缺失文件返回 []、坏行跳过。已本地完成验证：ruff `src/infrastructure/artifact test/infrastructure/artifact`（All checks passed）、pyright `src/infrastructure/artifact`（0 errors，test 目录亦 0 errors）、`PYTHONPATH=src uv run --frozen pytest test/infrastructure/artifact -q`（7 passed）。未偏离 design。已按 tasks.md 8.1/8.2/8.3 精确验收自查通过，勾选任务 8/8.1/8.2/8.3。评审工具恢复后需补评。

## 任务 9（config.local.properties 配置源支持 — common 改动）

- 任务 9 / attempt 1：拟请求 spec-evaluator 评审（改动 `src/common/configuration/configuration_utils.py`——新增 `_find_local_properties_file()`（读 `WORKSPACE_ROOT` 环境变量，空则 `Path.cwd()`，优先 `<base>/.epsilon/config.local.properties`，缺失兜底 `_find_file("config.local.properties")`）+ 模块级 `_LOCAL_PROPERTIES_FILE`；`settings_customise_sources` 返回顺序改为 `init, env, PropertiesFileSettingsSource(settings_cls, properties_path=_LOCAL_PROPERTIES_FILE), PropertiesFileSettingsSource(settings_cls), dotenv, file_secret`（env > local > properties > .env），复用既有源类不新增；`import os` 上提模块级；更新模块/类/方法中文 docstring 优先级说明。改动 `src/common/configuration/config_proxy.py`——`__init__` 的 `source_files` 追加 `_LOCAL_PROPERTIES_FILE`（存在时纳入 mtime 监听），既有双重检查锁定不变，更新类 docstring。新增 `test/common/configuration/test_configuration_local_properties.py`。当前执行上下文仍未启用 Agent/Task 工具（调用返回 "No such tool available: Task"），无法调用 spec-evaluator 子代理，沿用前序任务处理：本地严格自查 + 三项验证实跑。测试构造多源覆盖策略：复用既有 `test_id_validation_config_unit` 范式，构造隔离 `PropertiesBaseSettings` 子类并 override `settings_customise_sources` 把 local/properties 指向 tmp_path 临时文件、`.env` 用 `SettingsConfigDict(env_file=...)`、env 用 monkeypatch.setenv/delenv；`pytest.mark.parametrize` 覆盖 7 组「哪些源存在」组合断言命中最高优先级（Property 4），另加 local>properties、env>local 定向用例；缺失 local 文件用例断言不报错且退化到 properties/默认值（Property 5）；ConfigProxy mtime 用例 monkeypatch 模块级 `_find_file` 与 `_LOCAL_PROPERTIES_FILE` 指向 tmp 文件，经 `object.__getattribute__(proxy, "_source_files")` 断言存在时含 local、缺失时不含。已本地完成验证：ruff `src/common/configuration test/common/configuration`（All checks passed）、pyright `src/common/configuration`（仅 1 个既有无关 error：config_proxy.py:231 `create_config` 内 `config_class.hot_reload`，经 `git show HEAD:` 确认基线即存在、非本次引入）、`PYTHONPATH=src uv run --frozen pytest test/common/configuration -q`（33 passed，含新增 11 项与既有 config_proxy/id_validation 全部测试——既有配置类解析未因源顺序变更破裂，全绿）。未偏离 design 组件 10 与 ADR-0004。已按 tasks.md 9.1/9.2/9.3 精确验收自查通过，勾选任务 9/9.1/9.2/9.3。评审工具恢复后需补评。

## 任务 12（write_schema_meta）

- 任务 12 / attempt 1：拟请求 spec-evaluator 评审（新增 `src/infrastructure/storage/schema_meta.py`——模块级 `SCHEMA_VERSION = 1` 与 `def write_schema_meta(home: Path) -> None`，幂等写 `<home>/meta.json`、mkdir(parents=True, exist_ok=True)、版本一致跳过重写、任何 IO 异常 `logger.warning(..., exc_info=True)` 隔离不抛；新增 `test/infrastructure/storage/test_schema_meta.py`。未改动 storage 目录既有文件 local_file_tier_resolver.py 等，未碰其他任务文件）。当前执行上下文仍未启用 Agent/Task 工具（调用返回 "No such tool available: Agent"），无法调用 spec-evaluator 子代理。实现严格对齐 design 组件 9 骨架与数据模型 `.epsilon/meta.json` `{"schema_version": 1}`，仅依赖标准库（json/logging/pathlib），符合 ddd（纯 infrastructure）、code-documentation（模块+函数中文 docstring）、python-typing-lint（全量类型、无裸 Any）。已本地完成三项验证：ruff `src/infrastructure/storage/schema_meta.py test/infrastructure/storage/test_schema_meta.py`（All checks passed）、pyright `src/infrastructure/storage/schema_meta.py`（0 errors, 0 warnings）、`PYTHONPATH=src pytest test/infrastructure/storage/test_schema_meta.py -q`（8 passed）。幂等覆盖：mtime 断言重复调用不重写 + 版本不一致重写用例；故障隔离覆盖（Property 7）：monkeypatch Path.mkdir 抛 PermissionError、Path.write_text 抛 OSError、已有 meta.json 损坏非 JSON 三种场景均断言不抛。已按 tasks.md 精确验收自查通过，勾选任务 12/12.1/12.2。未偏离 design。评审工具恢复后需补评。

## 任务 11（Local_File_Log_Sink + SensitiveRedactionFilter + LogSinkConfig）

- 任务 11 / attempt 1：拟请求 spec-evaluator 评审（新增 `src/infrastructure/storage/log_sink_config.py`——`LogSinkConfig(PropertiesBaseSettings)`，`env_prefix="EPSILON_LOG_"`，字段 `to_file=True`/`level="INFO"`/`rotation_max_bytes=10_485_760`/`rotation_backup_count=5`；新增 `src/infrastructure/storage/local_file_log_sink.py`——`SensitiveRedactionFilter(logging.Filter)` 预编译 key[:=]value 正则、就地脱敏 `record.msg`/`record.args`（quoted 与 bare 两种取值均替换为 `****`）、恒返回 True 且脱敏异常兜底不影响写出，`configure_local_file_logging(tier_resolver, config, sensitive_keys, *, tier=StorageTier.USER)`——`to_file=False` 返 None，否则 `resolve(USER).logs_dir(create=True)` → `RotatingFileHandler(logs_dir/"epsilon.log", maxBytes, backupCount, encoding="utf-8")` + level + Filter + Formatter，挂 root logger；新增 `test/infrastructure/storage/test_log_sink_redaction.py`）。当前执行上下文仍未启用 Agent/Task 工具（调用返回 "No such tool available: Agent"），无法调用 spec-evaluator 子代理。未改动 `local_file_tier_resolver.py` 及其他任务文件。关键确认：日志经 `resolve(StorageTier.USER)` 落 `<user_base>/.epsilon/<project-hash>/logs/epsilon.log`（决策 2b），测试 `test_configure_logging_writes_to_user_tier` 断言项目工作区 `.epsilon/logs` 不存在，未落 PROJECT。handler 清理：测试用 `_cleanup_root_handlers` fixture teardown 移除本次新增的 root handler 并 close，避免污染其他测试。已本地完成验证：ruff `src/infrastructure/storage test/infrastructure/storage`（All checks passed）、pyright `log_sink_config.py local_file_log_sink.py`（0 errors）、`test/infrastructure/storage` 全量（34 passed，含既有 tier_resolver 测试未破裂）。相对 design 组件 8 骨架的偏离：脱敏正则由单一 `("?)([^"\s,&]+)(\2)` 改为 quoted/bare 双分支 + callback 替换，以正确处理引号内含空格的取值（如 `"authorization":"Bearer x"`），语义仍为「值替换为 ****、恒返回 True」，属健壮性增强；另对 `record.args` 亦做脱敏（design 未强制但任务书要求）。已按 tasks.md 精确验收自查通过，勾选任务 11/11.1/11.2/11.3。评审工具恢复后需补评。

## 任务 13（DI 装配：tier resolver + artifact store + 改 trace store 工厂）

- 任务 13 / attempt 1：拟请求 spec-evaluator 评审，改动 `src/application/container_config.py`（新增模块级 `_tier_resolver` 单例与 `_create_tier_resolver()`——PROJECT 基点取 `workspace_config.root` strip 后非空否则 `Path.cwd()`、惰性缓存、TYPE_CHECKING 导入 `LocalFileTierResolver`；改 `_create_trace_store()` 为注入 `tier_resolver=_create_tier_resolver()` 并保留 `trace_config.enabled` 关闭返回 None；新增 `_create_artifact_store()`——`artifact_config.enabled` 关闭返回 None 否则 `LocalFileArtifactStoreAdapter(tier_resolver=...)`；`configure_container()` 中 `_tier_resolver=None` 重置 + 并列注册 `ArtifactStorePort` 为 Scope.SINGLETON；`_init_local_persistence()` 末尾对 PROJECT tier home 调用一次 `write_schema_meta`），新增 `test/application/test_container_artifact_trace_wiring.py`（共享单例 / ARTIFACT_ENABLED=false / TRACE_ENABLED=false → None / tier resolver 缓存），并顺带修复 3 个既有失败测试（`test_container_config.py::test_create_agent_...`、`test_run_container_wiring_unit.py` 两个 `_create_agent` 收敛用例）——在其 fake resolve_map 补登 `TraceStorePort: None`（`_create_agent` 仅解析 TraceStorePort，不解析 ArtifactStorePort，故不补 artifact；断言本意未削弱）。当前执行上下文仍未启用 Agent/Task 工具（Agent 工具返回 "No such tool available: Agent"），无法调用 spec-evaluator 子代理。关键确认：`_create_agent` 只解析 `TraceStorePort`；artifact store 由后续 spec 写入方消费，本任务只做 DI 绑定；trace/artifact 均 Scope.SINGLETON，写读共享。禁用用例经 importlib 拿真子模块 monkeypatch（包 __init__ 同名重导出 artifact_config 实例遮蔽子模块）。已本地完成验证：ruff（All checks passed）、pyright `container_config.py`（仅 1 个 pre-existing error `get_session_factory`，已 git stash 验证改动前即存在，无本次新增）、`PYTHONPATH=src pytest test/application -q`（410 passed，含 3 个原失败测试转绿与既有容器装配测试全绿）。未偏离 design 组件 11。已按 tasks.md 精确验收自查通过，勾选任务 13/13.1/13.2。评审工具恢复后需补评。

## 任务 14（会话主状态 USER tier 默认迁移 — application + infrastructure 改动）

- 任务 14 / attempt 1：拟请求 spec-evaluator 评审。改动 `src/infrastructure/persistence/local_file/config/local_persistence_config.py`（14.1：`root` 默认值由 `"../.local_persistence/epsilon-boot"` 改为空串 `""`；更新 `root` 字段中文 docstring 说明空串=启用 USER tier 默认迁移、显式配置优先不迁移、redis 不生效；保留 `extra="forbid"`、`_reject_deprecated_ttl_env` 黑名单校验、`hot_reload=False` 不变）；改动 `src/application/container_config.py`（14.2：新增 `_LEGACY_LOCAL_PERSISTENCE_ROOT` 常量、`_ResolvedLocalPersistenceConfig` 生效视图类（仅覆盖 `root`，其余字段 `__getattr__` 透传底层配置，保证 `_validate_local_persistence_root` 全部校验不弱化）、`_resolve_local_persistence_config()`（分流：redis→原样透传不迁移；显式非空 root→原样透传不迁移；空 root 且非 redis→`_create_tier_resolver().user_persistence_root()` 迁移并触发提示）、`_emit_default_migration_hint()`（旧默认目录 `<CWD>/../.local_persistence/epsilon-boot` 存在且非空、新默认目录不存在或为空时 `logger.info` 输出中文提示含旧/新路径与手动迁移/显式设 LOCAL_PERSISTENCE_ROOT 两选项、不自动搬运、任何异常静默跳过降级 `logger.debug`）；`_init_local_persistence` 内 `_validate_local_persistence_root` 前先调 `_resolve_local_persistence_config()`，其余安全校验/TmpFileSweeper/schema_meta 不变）；新增 `test/application/test_local_persistence_default_migration.py`（6 用例）。当前执行上下文仍未启用 Agent/Task 工具（调用 spec-evaluator 返回 "No such tool available: Agent"），无法调用评审子代理，沿用前序任务处理：本地严格自查 + 三项验证实跑。分流落点：迁移判断置于 `_validate_local_persistence_root` 之前，只改「默认根解析」不改校验链；显式 root 与 redis 均直接返回底层 `local_persistence_config` 对象（`effective is local_persistence_config`），迁移场景返回覆盖 root 的 `_ResolvedLocalPersistenceConfig` 视图，validation 对新默认路径原样生效（相互包含冲突仍 fail-fast，用例 `test_explicit_root_still_fails_fast_on_workspace_conflict` 验证）。一次性提示：`test_first_start_migration_hint_emitted_without_moving_data` 用 monkeypatch.chdir 到 tmp 工作目录构造旧目录非空 + 新目录空，caplog 断言恰 1 条 INFO 提示含旧/新路径与 LOCAL_PERSISTENCE_ROOT 关键字，并断言新目录仍空、旧文件仍在（未搬运）；`test_no_hint_when_legacy_dir_absent` 断言旧目录缺失不提示。注意：任务 14.1 只改类字段默认为空串，但 `config.properties` 仍显式提供旧值（既有 `test_local_persistence_config_defaults` 仍断言旧值且通过），故迁移分流当前视为「显式配置」不激活，需待任务 16 将 config.properties 该行留空后方生效——与 tasks 编排一致。已本地完成验证：ruff `local_persistence_config.py container_config.py test_local_persistence_default_migration.py`（All checks passed，期间移除一处 non-enabled 的 `# noqa: BLE001`）、pyright `local_persistence_config.py container_config.py`（仅 1 个 pre-existing error `get_session_factory`，无本次新增）、`PYTHONPATH=src uv run --frozen pytest test/application -q`（416 passed，含 T13 wiring 测试与既有 persistence/startup 相关测试全绿；新增迁移测试 6 passed）。未偏离 design「迁移与兼容」与 ADR-0006 决策 1a/4/5。已按 tasks.md 14.1/14.2/14.3 精确验收自查通过，勾选任务 14/14.1/14.2/14.3。评审工具恢复后需补评。

## 任务 15（CLI 入口装配本地文件日志 — application 改动 cli/main.py）

- 任务 15 / attempt 1：拟请求 spec-evaluator 评审。改动 `src/application/cli/main.py`（15.1：新增 `import logging` + `logger = logging.getLogger(__name__)`；新增可测切片 `_configure_cli_file_logging()`——函数内惰性 import `RequestLoggingConfig` / `_create_tier_resolver` / `configure_local_file_logging` / `LogSinkConfig`，`sensitive_keys = frozenset(RequestLoggingConfig().get_sensitive_body_fields_set())`，调 `configure_local_file_logging(_create_tier_resolver(), LogSinkConfig(), sensitive_keys)`（tier 用函数默认 `StorageTier.USER`，不显式传 kwarg 行为等价）；整体 try/except 兜底，失败仅 `logger.warning` 不阻断 CLI；`_run_tui` / `_run_exec` 在 `async with CliRuntime() as runtime:` 之后立即调用该切片；`_run_serve` 不调用，既有 FastAPI 日志链路不变）；新增 `test/application/cli/test_cli_file_logging.py`（4 用例：装配后 root 挂唯一 RotatingFileHandler 且落临时 HOME 的 `.../logs/epsilon.log`（USER tier）、`EPSILON_LOG_TO_FILE=false` 不装配、`_create_tier_resolver` 抛错时故障隔离不传播且不挂 handler、serve 分支 monkeypatch 断言不调用 `_configure_cli_file_logging`；autouse fixture 在 teardown 移除+关闭本用例新增的 RotatingFileHandler，另一 fixture 重置 `container_config._tier_resolver=None` 并 setenv HOME + chdir 临时项目目录）。当前执行上下文仍未启用 Agent/Task 工具（调用 spec-evaluator 返回 "No such tool available: Agent"），无法调用评审子代理，沿用前序任务处理：本地严格自查 + 全部验证实跑。隔离 TUI 手法：不真正跑 `TuiApp(runtime).run()`，直接调用可测切片 `_configure_cli_file_logging()` 断言 handler 效果；serve 用例经 `main(["serve",...])` + monkeypatch `_run_serve`/`_configure_cli_file_logging` 断言 serve 路径零装配。未改 `container_config.py` 装配逻辑（T13/T14 范围）与 `runtime.py`。已本地完成验证：ruff `src/application/cli test/application/cli`（All checks passed）、pyright `src/application/cli`（0 errors）、`PYTHONPATH=src uv run --frozen pytest test/application/cli -q`（80 passed，新文件 4 passed）、`PYTHONPATH=src uv run --frozen pytest test/application -q`（420 passed，无回归）。已知无关 pre-existing error（value_objects.py 7 个、container_config.py get_session_factory 1 个）未触碰。未偏离 design 组件 8 装配点与 ADR-0005 / 需求 4.1/4.2 / Property 9。已按 tasks.md 15.1/15.2 精确验收自查通过，勾选任务 15/15.1/15.2。评审工具恢复后需补评。

## 任务 16（config.properties 配置键）

- 任务 16 / attempt 1：拟请求 spec-evaluator 评审（仅改 `epsilon-boot/config.properties`：新增 `ARTIFACT_ENABLED=true`；新增 `EPSILON_LOG_TO_FILE=true`/`EPSILON_LOG_LEVEL=INFO`/`EPSILON_LOG_ROTATION_MAX_BYTES=10485760`/`EPSILON_LOG_ROTATION_BACKUP_COUNT=5`，均置于 trace 配置块之后并附中文注释；`LOCAL_PERSISTENCE_ROOT=../.local_persistence/epsilon-boot` 一行改为注释留空 `# LOCAL_PERSISTENCE_ROOT=`（决策 1a），补充 USER tier 默认迁移说明、显式配置优先/redis 不生效、旧数据两选项搬迁指引，保留 NFS/SMB/OSS FUSE 与多容器共享禁止注释）。当前执行上下文仍未启用 Agent/Task 工具（沿用本 feature 既有条目：spec-evaluator 子代理不可用），无法调用子代理；已本地完成验证。
- 换行符修正：Edit 工具首次落盘时将整文件统一为 CRLF，误改动了原本 LF-only 的 6 行 `LOGGING_REQUEST_*`（仓库该文件为 CRLF/LF 混合，420/426 行 CRLF）；已用二进制精确回写把这 6 行还原为 LF，git diff 由 46 行收敛到 34 行，仅剩本任务意图内改动（29 增 5 删），无范围外改动。
- 验证：`PYTHONPATH=src uv run --frozen pytest test/common/configuration test/application/test_local_persistence_default_migration.py test/application/test_container_artifact_trace_wiring.py -q`（43 passed）；快速全量 `PYTHONPATH=src uv run --frozen pytest test/application test/infrastructure -q`（1965 passed, 2 skipped, 1 failed）。
- 既有测试受 blank 影响（按任务指示停下报告、不回退决策 1a）：`test/infrastructure/persistence/local_file/test_local_persistence_config_unit.py::test_local_persistence_config_defaults` 失败——该用例硬编码断言 `cfg.root == "../.local_persistence/epsilon-boot"`。因决策 1a 注释掉 config.properties 中该键、且任务 14.1 已将 `LocalPersistenceConfig.root` 代码默认值改为空串，故 `LocalPersistenceConfig()` 现返回 `""`，与旧硬编码断言冲突。这是决策 1a 的预期直接后果；该断言属任务 14（LocalPersistenceConfig 默认迁移）领域的伴随更新，超出任务 16「仅改 config.properties、不碰代码」范围，故不在本切片修改，留待任务 14 收尾或后续检查点（C4/C5）对齐为 `""`。未偏离 design 配置键章节与迁移与兼容/决策 1a。已勾选任务 16/16.1。评审工具恢复后需补评。

## 任务 18（文档同步 doc-sync）

- 任务 18 / 跳过 spec-evaluator：本切片为纯文档改动（`docs/configuration.md`、`docs/architecture.md`、`docs/tools.md`、`/workspace/TODO.md`），不新增/修改任何生产源码或测试文件，按 Main Loop 步骤 6 直接勾选，不请求评审。
- 18.1 `docs/configuration.md`：`LOCAL_PERSISTENCE_*` 行 `ROOT` 默认由旧绝对路径改为「默认留空」；新增「`LOCAL_PERSISTENCE_ROOT` 默认迁移（USER tier）」子节（留空→`~/.epsilon/persistence/<project-hash>/`、显式/ redis 优先不迁移、旧数据两种搬迁指引 + 首次启动一次性提示、安全禁令保留）；新增「本地覆盖配置 `config.local.properties`」章节（格式/`.epsilon/` 定位/优先级链 env>local>properties>.env/缺失不报错/不入库/不改主源约定）；新增「任务产物与本地文件日志」配置表（`ARTIFACT_ENABLED`/`EPSILON_LOG_TO_FILE`/`LEVEL`/`ROTATION_MAX_BYTES`/`ROTATION_BACKUP_COUNT` 默认值与代码/`config.properties` 一致）。
- 18.2 `docs/architecture.md`：Port→Adapter 表补 `TraceStorePort`/`ArtifactStorePort` 两行；新增「存储等级抽象（StorageTier）」章节（USER/PROJECT/预留 TENANT、Port 以 tier 为定位维度、`LocalFileTierResolver` 唯一 project-hash 生成点、tier→目录映射与归属矩阵、`write_schema_meta` 元数据、domain 只认 tier / 物理路径下沉 infrastructure / 云端同构可替换）。
- 18.3 `docs/tools.md`：新增「工具产物记录与 `ArtifactStorePort`」章节（抽象已就位、写入方待后续 spec 接入、可选注入零行为变化、不记录完整敏感内容）。索引核查：无 `docs/README.md`；主题索引在根 `CLAUDE.md`，`configuration.md`/`architecture.md`/`tools.md` 已登记，未新增主题文档，无需改索引。
- 18.4 `/workspace/TODO.md`：P0.2 标题标 ✅；子项按实际交付勾选——`.epsilon/` 目录规范 / `traces/` / `logs/` / `config.local.properties` / 云端同构 schema 勾 `[x]`；`.epsilon/sessions/` 与 `.epsilon/artifacts/` 标 `[~]` 部分完成（子目录抽象/Port/adapter 已交付，但会话摘要与 artifact 写入方属后续 spec，诚实反映）。「推荐 Spec 拆分」中 `local-trace-artifacts` 勾 `[x]`。P0.3 `ArtifactTrace` 值对象行勾 `[x]`（补齐 `ArtifactTrace` + `ArtifactStorePort` + 存储）；P0.3 `ErrorTrace` 异常路径行仍保留未勾（明确 Out of Scope）。
- 文档—代码一致性自检（grep）：`ARTIFACT_ENABLED`/`EPSILON_LOG_*`/`LOCAL_PERSISTENCE_ROOT` 键名、`~/.epsilon/<project-hash>/logs/`、`~/.epsilon/persistence/<project-hash>/`、`.epsilon/traces`、`.epsilon/artifacts` 路径与 `config.properties`/design 完全一致；「日志落 PROJECT / PROJECT/logs」旧表述仅存在于 ADR-0005（未采纳方案 B 记录）与 spec 输入文档，编辑的三篇主题文档无残留。未发现文档与代码不一致处。
