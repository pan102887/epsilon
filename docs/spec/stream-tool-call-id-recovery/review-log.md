# Review Log：流式工具调用 ID 兼容恢复

- Task 1.1-1.3 attempt 1: PASS（人工确认进入下一步）。变更 `ProviderConfig.stream_tool_call_id_strategy`、Provider 默认配置键与配置读取测试；验证命令 `uv run --frozen pytest test/infrastructure/model_access/test_provider_config_stream_tool_call_id_strategy_unit.py -q` 通过，4 passed。
- Task 2.1-2.4 attempt 1: PASS（本地 evaluator review）。`OpenAICompatibleAdapter` 的策略读取、合成 id、恢复日志、finished/usage-only materialize 与 metadata 均符合设计；验证命令 `uv run --frozen python -m compileall src/infrastructure/model_access/openai_compatible_adapter.py` 与 `uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_tool_calls_unit.py -q` 通过，5 passed。
- Task 3.1-3.4 attempt 1: PASS（本地 evaluator review）。新增恢复单测与属性测试，更新 strict/id validation 与 materialize 测试；验证命令 `uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_id_recovery_unit.py -q`、`uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_id_validation_unit.py test/infrastructure/model_access/test_openai_compatible_materialize_normalize_unit.py -q`、`uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_id_recovery_property.py -q` 均通过。
- Task 4.1-4.2 attempt 1: PASS（本地 evaluator review）。新增 ReAct + OpenAICompatibleAdapter 集成测试，验证合成 id 在 assistant tool_calls、工具执行入参、ToolMessage 以及 run_streaming tool_progress metadata 中保持一致，并验证 max_rounds=1 最终轮透传恢复 metadata；验证命令 `uv run --frozen pytest test/infrastructure/agent/test_react_agent_stream_tool_call_id_recovery_unit.py -q` 通过，3 passed。
- Task 5.1-5.2 attempt 1: PASS（本地 evaluator review）。在 `docs/agent.md` 记录 `MODEL_*_STREAM_TOOL_CALL_ID_STRATEGY`、`recover/raise`、`call_synthetic_` 前缀与排障日志字段，并新增文档静态测试；验证命令 `uv run --frozen pytest test/application/test_stream_tool_call_id_recovery_docs_static.py -q` 通过，1 passed。
- Task 6 attempt 1: PASS（本地 evaluator review）。完成模型接入、Agent 链路与全量回归检查；验证命令 `uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_id_recovery_unit.py -q` 通过，3 passed；`uv run --frozen pytest test/infrastructure/model_access/test_openai_compatible_stream_id_validation_unit.py test/infrastructure/model_access/test_openai_compatible_stream_tool_calls_unit.py -q` 通过，9 passed；`uv run --frozen pytest test/infrastructure/agent/test_react_agent_stream_tool_call_id_recovery_unit.py -q` 通过，3 passed；`uv run --frozen pytest test -q` 通过，1752 passed, 2 skipped。
