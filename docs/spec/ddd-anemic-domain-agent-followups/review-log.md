# Review Log — ddd-anemic-domain-agent-followups（Group A）

> 追加式历史，仅记录 spec-evaluator 复审与跳过决策，禁止覆盖既有条目。

- 环境说明：本次执行环境未启用 `spec-evaluator` 子代理（Agent/Task 工具不可用）。对需复审的实现 slice，改由 generator 依 requirement/design 做严格自评，并逐条记录等价性核对与门禁输出；结论作为审计留痕，非替代人工复审。

- 任务 1.1（config_policy.py，尝试 1）：自评 PASS。三分支逐一对照原 `_clamp_max_delegation_depth`：`raw is None` 原样返回（原实现 `raw is not None` 为假、不改动）；`int(raw) <= 0` 返回 3（原实现写回 `_DEFAULT_MAX_DELEGATION_DEPTH`）；`int(raw)` 抛 `TypeError`/`ValueError` 原样返回（原实现 `except: pass` 不改动，等价）；`int(raw) > 0` 原样返回（原实现不进 if 分支）。领域纯净度：仅 `from __future__ import annotations`，零 infra/框架/pydantic/json/logging/OTel/ContextVar。ruff/pyright 零错误。中文 docstring 齐备、含与 DelegationDepthPolicy 边界说明。禁裸 Any（入参 `object`，`int(raw)` 处窄豁免 `# type: ignore[call-overload]`）。

- 任务 1.2（test_config_policy_unit.py，尝试 1）：自评 PASS。脱离运行时单测，仅 import `domain.agent.config_policy`。覆盖 AC6.2 全分支：`None` 原样返回、`0`/`-5`/`"0"`/`"-3"` 归一为 3、`5`/`1`/`"7"`/`"10"` 保持、`"abc"`/`object()`/`list`/`dict`（触发 TypeError/ValueError）保留原值（`is` 断言）、`3.9` float 锚点保持、`default_max_delegation_depth()==3`。执行：15 passed；ruff All checks passed。

- 任务 1.3（agent_config.py validator 改委托，尝试 1）：自评 PASS。配置类留原位（pydantic-settings、`AGENT_` 前缀、`agent_config` 全局实例、字段与默认值不变）；`_DEFAULT_MAX_DELEGATION_DEPTH` 改指领域常量别名；validator 委托 `DelegationDepthNormalizationPolicy.normalize`，判据从 `raw is not None` 收敛为「键存在则归一」——等价性：键缺失两侧均不改动；键存在且值 None 时 `normalize(None)=None` 写回等价于不改动。`dict[str, Any]` 收窄为 `dict[str, object]`、移除未用 `from typing import Any`（禁裸 Any 等价调整）。ruff 触发 I001 已 `--fix`（拆为两条 import 语句，风格等价）；pyright 0 errors；targeted tests 37 passed（container_config / agent_delegation_config_properties / agent_config_validation / named_agent_config_properties）。

- 任务 2（CP-A 门禁，跳过 evaluator：纯只读校验、无源码改动）：全部门禁通过。① `import domain.agent.config_policy` OK；② `test_config_policy_unit.py` 15 passed；③ 领域纯净度 grep 零命中（`ContextVar|opentelemetry|OTel` 唯一命中在 docstring 描述性文字「无 ContextVar、无 OTel」，与既有 ADR-0014 样板 guardrail_policy.py 同源假阳性；import 行仅 `from __future__ import annotations`，代码/import 行零真实依赖）；④ 边界不修改：`git diff --name-only` 无 `src/domain/task/policy.py`、无 `segmented_execution/guardrail_policy/ports/value_objects`；⑤ ruff All checks passed；⑥ pyright 0 errors。综合回归 52 passed。

## Group C（候选 C：分段续跑判定平移）

- 环境说明同上：`spec-evaluator` 子代理不可用，需复审 slice 由 generator 依 requirement/design 做严格自评并记录门禁输出。

- 任务 5.1（新建 `domain/agent/segmented_orchestration.py`，尝试 1）：自评 PASS。整体平移 `infrastructure/agent/segmented_orchestration.py`，逐行字面等价核验：`SegmentContinuationDecision` frozen dataclass（`should_continue: bool`/`stop_reason: SegmentStopReason`）不变；`decide_next_segment` keyword-only 签名、8 参数默认值（`approval_required=False`/`tool_boundary_available=True`/`risk_gate_required=False`）不变；12 门判定顺序不变（completed→approval_required→continue_precondition_failed→tool_boundary_unavailable→risk_gate_required→auto_disabled→max_continuations→total_token→total_duration→consecutive_paused→no_progress→repeated_tool_call）；全部 `>=` 运算符、`max_total_tokens is not None`/`max_duration_seconds is not None` 短路、`max_duration_seconds * 1000`、每条 `stop_reason` 返回值字面不变；全未触发返回 `SegmentContinuationDecision(True, "completed")`。导入集合与平移前完全一致（`from dataclasses import dataclass` + 四个 segmented_execution 值对象 + `from __future__ import annotations`），仅模块 docstring 更新为「自 infrastructure 平移至领域层同子域（ADR-0015）」。领域纯净度：import 行仅 `__future__`/`dataclasses`/`domain.agent.segmented_execution`，零 infra/框架/pydantic/json/logging/OTel/ContextVar/事件构件。import OK；ruff All checks passed；pyright 0 errors。

- 任务 5.2（新建 `test/domain/agent/test_segmented_orchestration_unit.py`，尝试 1）：自评 PASS。脱离运行时单测，仅 import `domain.agent.segmented_orchestration`/`domain.agent.segmented_execution`（同一对象专项额外 import 垫片模块用于身份断言，符合 AC5.3）。覆盖：6 条前置门（completed/approval_required 两路含 `status=="approval_required"` 与 `approval_required=True`/continue_precondition_failed/tool_boundary_unavailable/risk_gate_required）；auto_disabled；6 条预算与反循环门（max_continuations/total_token/total_duration(`×1000`)/consecutive_paused/no_progress/repeated_tool_call，均 `>=` 命中）；None 阈值短路（`max_total_tokens=None`+大 token、`max_duration_seconds=None`+大 elapsed_ms 均不命中）；全未触发 `== SegmentContinuationDecision(True,"completed")`（AC6.4）；垫片同一对象断言 `infra_so.SegmentContinuationDecision is dom_so.SegmentContinuationDecision` 且函数同一对象（Property 8/AC5.3）。执行 17 passed；ruff All checks passed。

- 任务 5.3（`infrastructure/agent/segmented_orchestration.py` 降 re-export 垫片，尝试 1）：自评 PASS。整文件替换为 docstring（标注平移至领域层 ADR-0015、垫片范式 ADR-0011/0014、同一类/函数对象 isinstance/== 不破裂）+ `from __future__ import annotations` + `from domain.agent.segmented_orchestration import (SegmentContinuationDecision, decide_next_segment)` + `__all__`。移除原判定实现（dataclass 定义、`decide_next_segment` 方法体、`from dataclasses import dataclass` 及分段值对象 import）。垫片可解析；同一对象断言 SAME-OBJECT OK；ruff/pyright 零错误。

- 任务 5.4（消费方与既有 infra 单测 import，尝试 1，generator 自判）：经垫片零改。`chat_service_adapter.py:64`、`task_agent_adapter.py:56` 与 `test/infrastructure/agent/test_segmented_orchestration_unit.py:12` 的 `from infrastructure.agent.segmented_orchestration import decide_next_segment` 经垫片可用，ruff check 全绿未触发未使用/私有规则，故 import/断言/时序一律未改（符合 AC4.3/AC5.2）。验证套件 26 passed（infra 单测 + chat_service_adapter 单测 + segmented_container_wiring_static）。

- 任务 6（CP-C 门禁，跳过 evaluator：纯只读校验、无源码改动）：全部门禁通过。① `import domain.agent.segmented_orchestration` OK；② 领域单测 17 passed；③ 领域纯净度 grep 零命中（forbidden imports rc=1、ContextVar/otel/events rc=1；import 行仅 `__future__`/`dataclasses`/`domain.agent.segmented_execution` 与平移前一致）；④ 垫片无残留判定 grep rc=1（`def decide_next_segment`/`should_continue >=`/`usage.`/`policy.max_` 零命中）；⑤ 边界不修改：`git diff --name-only` 无 `src/domain/task/policy.py`（OK）、无 `src/domain/agent/segmented_execution.py`（OK）；⑥ ruff All checks passed；pyright 0 errors。垫片同一对象 SAME-OBJECT OK；既有 infra 单测全绿（30 passed 与 domain 单测合并）。

## Group B（候选 B：审批默认查表上提）

- 环境说明同上：`spec-evaluator` 子代理在本执行环境不可用，需复审 slice 改由 generator 依 requirement/design 做严格自评并记录等价性核对与门禁输出。

- 任务 3.1（approval_lookup.py，尝试 1）：自评 PASS。常量字面自 `approval_policy_provider.py` 逐字迁移（`APPROVE_REJECT`/`APPROVE_EDIT_REJECT`/6 条 `DEFAULT_POLICIES` 键值与风险标签/`LOW_RISK_TOOLS` 4 工具）。`policy_for` 与原 provider 无 override 默认分支逐一等价：命中 `DEFAULT_POLICIES` → `interrupt=True`+`frozenset(decisions)`+`risk_label`；未命中 → `interrupt=False`+空决策集+`risk_label`（`in LOW_RISK_TOOLS` 则「低风险工具」否则 `""`）。`decisions_for` 与 `_DEFAULT_POLICIES.get(tool_name, (_APPROVE_REJECT, "用户配置审批工具"))` 逐值等价。领域纯净度：仅 import `from __future__` 与 `domain.agent.value_objects`，零 infra/框架/pydantic/**json**/logging/OTel/ContextVar。中文 docstring 齐备、显式记 JSON 解析留 infra（ADR-0008）。ruff All checks passed；pyright 0 errors（`frozenset(decisions)` 处采用 Group A 同款窄豁免 `# type: ignore[arg-type]`，规避 `ApprovalPolicy.allowed_decisions: frozenset[ApprovalDecisionType]` 与常量 `frozenset[str]` 的 Literal 收窄不匹配、避免裸 Any 越界）。

- 任务 3.2（test_approval_lookup_unit.py，尝试 1）：自评 PASS。脱离运行时单测，仅 import `domain.agent.approval_lookup`（断言经 policy 对象字段完成，无需 import value_objects）。覆盖 AC6.3 全分支：6 个 `DEFAULT_POLICIES` 工具（5×`APPROVE_REJECT` + `http_request`×`APPROVE_EDIT_REJECT`，逐值断言 `interrupt`/`allowed_decisions`/`risk_label`）；`LOW_RISK_TOOLS` 4 工具（不中断/空决策集/「低风险工具」）；未命中且非低风险（不中断/空/`""`）；`decisions_for` 命中与未命中默认元组；`DEFAULT_POLICIES` 键集合等价。执行 16 passed；ruff All checks passed。

- 任务 3.3（approval_policy_provider.py 委托改造，尝试 1）：自评 PASS。类身份/构造签名 `(enabled, interrupt_on)`/`ApprovalPolicyPort` 继承/`json`/`HitlConfigInvalidError`/`_VALID_DECISIONS` 均保留。`policy_for` 的 `enabled=False` 与 override 命中分支留原不变，无 override 默认两分支替换为 `return ApprovalDefaultLookup.policy_for(tool_name)`。`_policy_from_value` 的 `value is True` 分支改为 `decisions, risk_label = ApprovalDefaultLookup.decisions_for(tool_name)`，其余构造字面不变；`value is False`/`list`/`dict` 分支、`_parse_interrupt_on`/`_validate_decisions`、`HitlConfigInvalidError` 抛出条件与消息字面不变。**偏差记录**：tasks.md 3.3 要求「常量改别名 re-export（`DEFAULT_POLICIES as _DEFAULT_POLICIES` 等，保护既有私有名引用）」；实际全仓 grep 确认无任何 test/src 引用 provider 的 `_DEFAULT_POLICIES`/`_APPROVE_REJECT`/`_APPROVE_EDIT_REJECT`/`_LOW_RISK_TOOLS` 私有名（`test_approval_mode_unit.py` 的同名符号为其自身局部定义），保留 4 个别名 import 将触发 ruff F401「imported but unused」硬门禁失败。取「零新增 ruff 错误」硬约束优先，仅 import `ApprovalDefaultLookup`、删除 4 个未被引用的别名（行为等价、既有 provider 单测/属性测试零改全绿）。既有 provider 单测/属性测试 11 passed，断言未改。pyright：provider 从基线 2 个 `reportArgumentType` 降为 1 个（`policy_for` 分支上提领域并窄豁免、`_policy_from_value` value-is-True 分支基线错误保留），零净新增错误（`git stash` 核验基线为 2）。

- 任务 4（CP-B 门禁，跳过 evaluator：纯只读校验、无源码改动）：全部门禁通过。① `import domain.agent.approval_lookup` OK；② `test_approval_lookup_unit.py` 16 passed；③ 领域纯净度 grep：import/ContextVar/OTel/event 真实依赖零命中（`json`/`ContextVar` 唯一命中在 docstring 描述文字，同 Group A 假阳性；import 行仅 `__future__` + `domain.agent.value_objects`）；④ JSON 解析仍在 infra：三方法 + `import json` 在 provider、领域侧零真实 json 命中；⑤ Port 签名 `policy_for(self, tool_name: str) -> ApprovalPolicy` 未改、ctor `(enabled: bool, interrupt_on: str)` 未改、`test_container_config.py` 16 passed；⑥ ruff All checks passed；pyright 零净新增错误。

## Group Z（ADR-0015 + 文档同步 + 最终门禁）

- 环境说明同上：`spec-evaluator` 子代理在本执行环境不可用；Group Z 全部为文档（ADR / doc-sync）与只读最终门禁，无任何源码改动，按 Main Loop step 6 属跳过 evaluator 范畴（纯文档 + 只读校验），改由 generator 自评并逐条记录门禁实际输出。

- 任务 7.1（新增 ADR-0015，跳过 evaluator：纯文档、无源码）：完成。先 `ls docs/adr/` 核验 0015 未占用（当前最新 0014）。新建 `docs/adr/0015-uplift-agent-config-normalization-approval-lookup-and-relocate-segment-continuation.md`，遵循 `0000-template.md` 四段式 + 与 `0014-*.md` 一致的 front matter 与回链风格。front matter：`status: Accepted`、`date: 2026-07-07`、`deciders: [后端架构维护者]`、`supersedes:` 留空（**不 supersede ADR-0001**）。正文含：背景（ADR-0014 后续影响显式列出三候选、`Domain_Logic_In_Infrastructure`、与 ADR-0009/0014 同源）、决策 A/B/C（三候选落点与判据逐一字面等价）、两处边界厘清（A vs `DelegationDepthPolicy`、C vs `TaskContinuationPolicy`，均不合并不修改后者）、留 infrastructure 取舍（`AgentRuntimeConfig` 依赖 pydantic-settings、审批 JSON 解析依赖 `json` 按 ADR-0008 留 infra）、后果（脱离运行时单测、C 垫片临时并存留后续片、不引第三方依赖 / 领域事件）、备选方案 A–E 与未采纳原因；回链 ADR-0009 / ADR-0014 / ADR-0008 / ADR-0001。未改 0001–0014 任一 ADR 正文。验证：`test -f` OK；`grep supersedes:` 值为空；四条回链命中 11 次；`grep 0015 README.md` 命中。

- 任务 7.2（更新 `docs/adr/README.md` 索引，跳过 evaluator：纯文档）：完成。在 0014 行之后追加 0015 索引行（编号链接 / 标题「在 domain/agent 上提委派深度规范化与审批默认查表、平移分段续跑判定（充血化后续片）」/ `Accepted` / `2026-07-07`），仅追加一行、未改既有行。

- 任务 7.3（doc-sync 主题文档，跳过 evaluator：纯文档）：完成。先 grep 既有 ADR-0014 在两文档的同步粒度与措辞，保持一致风格，仅**新增指向性说明**、不重写章节：`docs/domain-model.md` 护栏领域服务段落后补一段（`config_policy.py::DelegationDepthNormalizationPolicy` + `DEFAULT_MAX_DELEGATION_DEPTH`、`approval_lookup.py::ApprovalDefaultLookup` + 常量、`segmented_orchestration.py::decide_next_segment` 平移至领域层 + infra 垫片、两处边界不合并、`AgentRuntimeConfig`/审批 JSON 解析因依赖边界留 infra 并回链 ADR-0015/0008）；`docs/architecture.md` 在护栏 ADR-0014 句后补一句同粒度指向性说明。验证：`grep -rl` 两文档均命中三领域构件名。

- 任务 8（CP-最终 门禁，跳过 evaluator：纯只读校验、无源码改动）：全部门禁通过，逐条实际输出——
  - Property 11 全量测试：`PYTHONPATH=src uv run --frozen pytest` → **3023 passed, 3 skipped, 1 warning**（与基线一致、无新增 failed）。
  - Property 1/3/4/5 判定等价：三领域新单测 + 既有 infra 单测（`test_config_policy_unit` / `test_approval_lookup_unit` / `test_segmented_orchestration_unit`(domain) / `test_approval_policy_provider_unit` / `_property` / `test_segmented_orchestration_unit`(infra)）→ **72 passed**。
  - Property 8 垫片同一对象：`i.SegmentContinuationDecision is d.SegmentContinuationDecision and i.decide_next_segment is d.decide_next_segment` → **SAME-OBJECT-OK**。
  - Property 7 契约 / 时序：`test_chat_service_adapter_unit` / `test_container_config` / `test_agent_delegation_config_properties` / `test_segmented_container_wiring_static` → **37 passed**；`ApprovalPolicyPort.policy_for` 签名 `def policy_for(self, tool_name: str) -> ApprovalPolicy`（ports.py:158）未改。
  - Property 9 三领域文件零基础设施依赖：forbidden imports grep rc=1（零命中）、event 构件 grep rc=1（零命中）；`ContextVar|opentelemetry` 唯一命中在 `config_policy.py`/`approval_lookup.py` 的 docstring 描述文字（同 Group A/B 已记录假阳性），import 行仅 `__future__` + `dataclasses`(仅 segmented) + `domain.agent.value_objects`(仅 approval_lookup) + `domain.agent.segmented_execution`(仅 segmented)，代码 / import 零真实基础设施依赖。
  - Property 2/6 两处边界不修改：`git diff --name-only | grep src/domain/task/policy.py` → **OK**（无 diff，`DelegationDepthPolicy`/`TaskContinuationPolicy` 未被修改）。
  - Property 10 不引事件 / 新依赖：event 构件 grep 零命中；`git diff --name-only | grep -E "pyproject.toml|uv.lock"` rc=1（**零命中，依赖清单不变**）。
  - 规范合规：`ruff check`（6 文件）→ **All checks passed!**；`pyright`（3 领域文件）→ **0 errors, 0 warnings, 0 informations**。
  - 范围锁定：`git diff --name-only` 源码改动仅 `src/infrastructure/agent/{agent_config,approval_policy_provider,segmented_orchestration}.py`（A/B 委托、C 垫片）+ 新增 `src/domain/agent/{config_policy,approval_lookup,segmented_orchestration}.py` + `test/domain/agent/` 三新单测；文档仅落 `docs/`（ADR-0015 新增 + README/domain-model/architecture）；**未改** `guardrail_policy.py`/`ports.py`/`value_objects.py`/`segmented_execution.py`/`agent_loop_*`/`domain/task/policy.py`/`pyproject.toml`/`uv.lock`。
  - ADR 存在性：`docs/adr/0015-*.md` 存在且 `grep 0015 README.md` 命中。
  - 结论：Group Z 全部任务（7 / 7.1 / 7.2 / 7.3 / 8）自评 PASS，本 spec `ddd-anemic-domain-agent-followups`（三候选 + 收尾）全部完成，无未决问题、无偏差。
